@import 'microo-theme/src/colours.pcss';

.exampleEdit{
  color: var(--dark-text);
  background-color: var(--light-background);
  padding: 1em 1em 1em 1.5em;
  margin: 10px 0 5px 0;
  border-radius: 0.2em;
  box-shadow: rgba(0,0,0,0.2) 0 3px 10px, inset 15px 0 0 0 white;
}